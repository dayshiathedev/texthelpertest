# Text Helper

Simple Python script that:
- Counts characters and words
- Shows full pages using integer division
- Calculates leftover characters and words
- Replaces words in the text

Run with:

```bash
python text_helper.py


4. Create another new file: `.gitignore`  
5. Put this inside:

```text
__pycache__/
*.py[cod]
.vscode/
.env
.venv
